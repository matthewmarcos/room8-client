import React from 'react';

const Landing = (props) => {

    return (
        <div className="landing container-fluid">
            Landing page here 
        </div>
    );

};

export default Landing;
